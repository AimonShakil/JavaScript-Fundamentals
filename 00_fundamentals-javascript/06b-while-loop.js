
let index = 0
while (index <=10 ) {
    console.log(` value of Index is ${index}`);
    index = index + 2 
}

let myArr = ["batman", " superman", "ironMan"]
    let arr = 0
    while (arr < myArr.length) {
        console.log(`value is ${myArr[arr]}`);
        arr = arr + 1
    }

// do while loop

let score = 1
do {
    console.log( `score is ${score}`);
    score ++    
    
} while (score <= 10 );
